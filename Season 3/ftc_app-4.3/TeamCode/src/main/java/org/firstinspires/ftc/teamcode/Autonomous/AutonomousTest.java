/*package org.firstinspires.ftc.teamcode.Autonomous;

import com.qualcomm.robotcore.eventloop.opmode.TeleOp;
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcore.eventloop.opmode.Autonomous;
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcore.hardware.HardwareMap;
import com.qualcomm.robotcore.util.Hardware;
import org.firstinspires.ftc.teamcode.*;
import org.firstinspires.ftc.teamcode.Vuforia.*;
import org.firstinspires.ftc.teamcode.Modules.*;

@Autonomous(name = "Test Autonomie", group = "Teste")

public class AutonomousTest extends LinearOpMode {

    Movement mov = new Movement();
    vuforia_class vf = new vuforia_class();

    @Override public void runOpMode() throws /*my hands up in the air InterruptedException {

        initRobot();
        while(!isStarted()) {
            idle();
        }
        
        
        mov.move(Math.PI/6, 50);
        sleep(1000);
        mov.move(Math.PI/6*7, 50);
        sleep(1000);
        
        
        
        double p1 = Math.sin(Math.PI/6 + Math.PI/4)/2;
        double p2 = Math.cos(Math.PI/6 + Math.PI/4)/2;
        mov.wheels.runWithoutEncoder();
        mov.wheels.setPower(-p1, p2, -p2, p1);
        sleep(2500);
        mov.wheels.stop();
    }

    public void initRobot() {
        mov.initMovement(hardwareMap);
        mov.op = this;
        mov.wheels.runToPosition();
    }
}
*/
